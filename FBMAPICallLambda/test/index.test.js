import { describe, it } from 'vitest';

describe('handler', () => {
  it('processes only active searches and returns a successful response', () => {});

  it('sends sorted newest results and limits payload to five entries', () => {});
});
