import { describe, it } from 'vitest';

describe('db-crud-service', () => {
  it('scans searches and unmarshalls returned items', () => {});

  it('updates newest offer field with remapped offer data', () => {});
});
