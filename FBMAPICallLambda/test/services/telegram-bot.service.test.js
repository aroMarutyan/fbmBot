import { describe, it } from 'vitest';

describe('telegram-bot-service', () => {
  it('sends plain and HTML responses to configured chat', () => {});

  it('formats and sends each result as HTML', () => {});
});
