import { describe, it } from 'vitest';

describe('firstCall', () => {
  it('returns first page items and builds search URL with filters', () => {});

  it('follows next pages until a non-empty page is found', () => {});

  it('stops searching after max next page limit is reached', () => {});

  it('records fetch and first call errors when the API call fails', () => {});
});
