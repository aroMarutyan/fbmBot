import { describe, it } from 'vitest';

describe('handler', () => {
  it('routes /ls to listSearches', () => {});

  it('routes /gl to getNewestResults', () => {});

  it('routes /ns to createNewSearch', () => {});

  it('routes /us to updateSearch', () => {});

  it('routes /ds to deleteSearch', () => {});

  it('responds with help text for /help command', () => {});

  it('responds with unrecognized command fallback', () => {});
});
