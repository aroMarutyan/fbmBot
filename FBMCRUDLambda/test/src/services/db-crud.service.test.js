import { describe, it } from 'vitest';

describe('db-crud.service', () => {
  it('lists active searches only and returns no-results message when none are active', () => {});

  it('fetches newest results for a specific search id', () => {});

  it('creates a search and sends confirmation with created search details', () => {});

  it('updates a search and sends the updated payload', () => {});

  it('deletes a search after validation and confirms deletion', () => {});

  it('sends error response when listing searches fails', () => {});
});
