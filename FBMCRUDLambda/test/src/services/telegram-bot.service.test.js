import { describe, it } from 'vitest';

describe('telegram-bot.service', () => {
  it('sends plain and html responses to the configured chat id', () => {});

  it('builds telegram response payload from offer details', () => {});
});
